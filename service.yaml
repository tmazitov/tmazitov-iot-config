apiVersion: v1
kind: Service
metadata:
  name: iot-app
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: iot-app
  ports:
    - name: http
      port: 8888        # Service port — the one port-forward points at
      targetPort: 8080  # port the application listens on
